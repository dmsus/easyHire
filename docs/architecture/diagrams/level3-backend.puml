@startuml
skinparam componentStyle uml2
title Backend API Components

package "Backend API" {
  component "Authentication Handler" as auth
  component "Assessment Handler" as assessment
  component "Question Handler" as question
  component "Scoring Engine" as scoring
  component "Code Execution Client" as code_client
}

database "PostgreSQL" as db {
  folder "User Repository" as user_repo
  folder "Assessment Repository" as assess_repo
  folder "Question Repository" as question_repo
  folder "Result Repository" as result_repo
}

auth --> user_repo : reads/writes
assessment --> assess_repo : reads/writes
question --> question_repo : reads/writes
scoring --> result_repo : reads/writes

assessment --> scoring : calls for scoring
assessment --> code_client : submits code

note right of scoring
Fibonacci-based scoring:
Level weights: 1,2,3,5
Thresholds: 8,21,55,144
end note

@enduml
